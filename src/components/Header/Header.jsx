import React from 'react';
import s from './Header.module.css';

const Header = () => {
    return (
        <h1 className={s.title} >
            Todo list
        </h1>
    )
}

export default Header;